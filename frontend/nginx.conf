server {
  listen 80;
  server_name _;
  # Angular's application builder places the browser bundle in this directory.
  root /usr/share/nginx/html/browser;
  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }
}
